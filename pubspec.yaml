name: blossom_cache
description: A network-free local Blossom blob store for Dart. Works on web, native, and server.
version: 0.2.0
repository: https://github.com/nogringo/blossom-cache

topics:
  - blossom
  - nostr
  - cache
  - storage
  - blob

environment:
  sdk: ^3.11.5

dependencies:
  crypto: ^3.0.7
  idb_shim: ^2.9.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
